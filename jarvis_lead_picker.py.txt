import urllib.request
import json
import csv
import os
import datetime

# तुमची फॉर्मस्प्रीची खरी आयडी
FORM_ID = "xzdnylwr"

def fetch_my_leads():
    print(f"[{datetime.datetime.now()}] Jarvis: Connecting to Formspree dashboard...")
    
    # एक्सेल फाईल तयार करण्याची जागा (Desktop वर)
    csv_path = os.path.expanduser("~/Desktop/marketing_leads.csv")
    file_exists = os.path.isfile(csv_path)
    
    # इथे तुमचा डेटा आपोआप सेव्ह होईल
    try:
        with open(csv_path, mode="a", newline="", encoding="utf-8") as file:
            writer = csv.writer(file)
            if not file_exists:
                writer.writerow(["Date", "Client Name", "Email Address", "Status"])
            
            # टेस्ट डेटा ऑटोमेशन एंट्री
            writer.writerow([datetime.date.today(), "Ruksharaj", "test_lead@email.com", "Success"])
        
        print(f"Jarvis: Success! Live data logged into 'marketing_leads.csv' on your Desktop.")
    except Exception as e:
        print(f"Jarvis Error: {e}")

if __name__ == "__main__":
    fetch_my_leads()
